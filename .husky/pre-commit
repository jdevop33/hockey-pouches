#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to lint and format staged files
npx lint-staged

# Run schema import fixer to detect and fix common schema import issues
node ./scripts/fix-schema-imports.mjs 